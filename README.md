# Forum Community - Современный форум на Django

[![Python Version](https://img.shields.io/badge/python-3.10-blue.svg)](https://www.python.org/)
[![Django Version](https://img.shields.io/badge/django-4.2.7-green.svg)](https://www.djangoproject.com/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

Полнофункциональный форум с системой ролей, модерацией, личными сообщениями и современным дизайном.

## 🚀 Возможности

### Основной функционал
- ✅ Регистрация и аутентификация пользователей
- ✅ Система ролей (Пользователь, Модератор, Администратор)
- ✅ Создание категорий, тем и сообщений
- ✅ Система лайков и рейтингов
- ✅ Личные сообщения между пользователями
- ✅ Профили пользователей с аватарами
- ✅ Поиск по форуму
- ✅ Прикрепление файлов к сообщениям
- ✅ Система жалоб и модерация
- ✅ Markdown поддержка для форматирования
- ✅ Теги для тем

### Технические возможности
- 🐳 Docker и Docker Compose поддержка
- 🗄️ PostgreSQL база данных
- 🚀 Redis для кеширования
- 🌐 Nginx для reverse proxy
- 📊 Админ-панель Django
- 🎨 Адаптивный дизайн (Bootstrap 4)
- ⚡ Оптимизированные запросы к БД
- 🔒 Безопасность (CSRF, XSS защита)

## 📋 Требования

- Python 3.10+
- PostgreSQL 13+
- Redis 6+
- Docker & Docker Compose (опционально)

## 🛠️ Быстрая установка с Docker

### 1. Клонирование репозитория

```bash
git clone https://github.com/cheltenhemm-jpg/forum2.git
cd forum2
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
# Отредактируйте .env файл, установите SECRET_KEY и другие параметры
```

### 3. Запуск через Docker Compose

```bash
docker-compose up -d
```

Форум будет доступен по адресу: http://localhost

**Учетные данные по умолчанию:**
- Логин: `admin`
- Пароль: `admin`

## 📦 Установка без Docker (Ubuntu 22.04)

### Автоматическая установка

```bash
sudo bash install.sh
```

### Ручная установка

#### 1. Установка зависимостей

```bash
sudo apt-get update
sudo apt-get install -y python3.10 python3-pip python3-venv postgresql redis-server nginx
```

#### 2. Настройка PostgreSQL

```bash
sudo -u postgres psql
CREATE DATABASE forum_db;
CREATE USER forum_user WITH PASSWORD 'forum_password';
GRANT ALL PRIVILEGES ON DATABASE forum_db TO forum_user;
\q
```

#### 3. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### 4. Настройка проекта

```bash
cp .env.example .env
# Отредактируйте .env файл

python manage.py migrate
python manage.py createsuperuser
python manage.py collectstatic
```

#### 5. Запуск сервера

```bash
# Для разработки
python manage.py runserver

# Для продакшена
gunicorn forumsite.wsgi:application --bind 0.0.0.0:8000
```

## 🏗️ Структура проекта

```
forum2/
├── accounts/               # Приложение пользователей
│   ├── models.py          # Модель User, UserProfile
│   ├── forms.py           # Формы регистрации/профиля
│   ├── views.py           # Views для аутентификации
│   └── admin.py           # Админ-панель пользователей
├── forum/                  # Основное приложение форума
│   ├── models.py          # Category, Thread, Post, Like
│   ├── views.py           # Views форума
│   ├── forms.py           # Формы форума
│   └── admin.py           # Админ-панель форума
├── moderation/             # Приложение модерации
├── templates/              # HTML шаблоны
│   ├── base.html          # Базовый шаблон
│   ├── forum/             # Шаблоны форума
│   └── accounts/          # Шаблоны аутентификации
├── static/                 # Статические файлы
│   ├── css/               # CSS стили
│   └── js/                # JavaScript
├── forumsite/              # Настройки Django
│   ├── settings.py        # Настройки проекта
│   ├── urls.py            # URL маршруты
│   └── wsgi.py            # WSGI конфигурация
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose конфигурация
├── requirements.txt        # Python зависимости
└── README.md              # Документация
```

## 🎨 Использование

### Создание категорий

Категории создаются через админ-панель:
1. Перейдите в `/admin/`
2. Выберите "Категории" → "Добавить категорию"
3. Заполните название, описание и иконку

### Управление пользователями

#### Назначение ролей
В админ-панели можно назначить роли пользователям:
- **User** - обычный пользователь
- **Moderator** - может редактировать/удалять посты, банить пользователей
- **Admin** - полный доступ к управлению

### API Endpoints

Форум предоставляет следующие URL:

```
/                          # Главная страница
/category/<slug>/          # Просмотр категории
/thread/<slug>/            # Просмотр темы
/thread/create/            # Создание темы (требует авторизации)
/post/<id>/like/           # Лайк/дизлайк поста
/search/                   # Поиск
/accounts/login/           # Вход
/accounts/signup/          # Регистрация
/accounts/profile/<username>/  # Профиль пользователя
/messages/                 # Личные сообщения
/admin/                    # Админ-панель
```

## 🧪 Тестирование

Запуск тестов:

```bash
python manage.py test
```

Запуск с покрытием:

```bash
pip install coverage
coverage run manage.py test
coverage report
coverage html  # Генерация HTML отчета
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
# Django
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database
DB_ENGINE=django.db.backends.postgresql
DB_NAME=forum_db
DB_USER=forum_user
DB_PASSWORD=forum_password
DB_HOST=db
DB_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0

# Email (опционально)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-password

# Site
SITE_NAME=Forum Community
```

### Настройка Nginx для продакшена

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        alias /var/www/forum/staticfiles/;
    }

    location /media/ {
        alias /var/www/forum/media/;
    }
}
```

## 📊 Производительность

- Использование `select_related` и `prefetch_related` для оптимизации запросов
- Redis кеширование для сессий и частых запросов
- WhiteNoise для эффективной раздачи статики
- Индексы БД для быстрого поиска
- Пагинация для больших списков

## 🔐 Безопасность

- CSRF защита
- XSS защита через escape HTML
- Защита от SQL injection (Django ORM)
- Secure cookies в продакшене
- Валидация всех форм
- Ограничение размера загружаемых файлов

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта!

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 📧 Контакты

- GitHub: [@cheltenhemm-jpg](https://github.com/cheltenhemm-jpg)
- Repository: [https://github.com/cheltenhemm-jpg/forum2](https://github.com/cheltenhemm-jpg/forum2)

## 🙏 Благодарности

- Django Framework
- Bootstrap
- Font Awesome
- PostgreSQL
- Redis
- Все контрибьюторы проекта

---

Made with ❤️ by AI Assistant
